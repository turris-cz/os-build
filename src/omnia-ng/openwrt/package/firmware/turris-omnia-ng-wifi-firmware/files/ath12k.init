#!/bin/sh /etc/rc.common

START=03
STOP=94

boot() {
        if ! echo -n "/lib/firmware/ath12k/ini" > /sys/module/firmware_class/parameters/path; then
                echo "Failed to set path for ini framework" > /dev/console
        fi
}
